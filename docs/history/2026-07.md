# Identity-free workspace (W1–W3) + ЕКГ paragraph block + obektivno source highlight (2026-07-20/23)

The workspace stops asking who the patient is. Alongside it: the two new investigation-block
mirrors, a paragraph render mode for light blocks, and G7's per-clause provenance wired into
the Обективно highlight. Backend twins are in `tubermed-backend/docs/history/2026-07.md`
(identity removal Phase A, ekg-v1, G7).

- **W1 (`45b0dac`).** `VisitHeaderStrip` replaces `PatientHeaderStrip` — the header is rebuilt
  from the consultation row's OWN metadata (created_at, visit type, chief complaint), and the
  stepper's first stage is „Преглед", not „Пациент". `use-cold-start-recovery.ts` no longer
  fetches a patient: a `patient_id`-NULL row is fully recoverable from the `?visit=` row alone.
- **W2 (`6076cf4`).** `StartVisitCard` — one click from empty page to recording: visit type +
  template + optional chief complaint + „Започни запис", with **no submit gate and no identifier
  field**. Staging POSTs no `patient_id`; `sessionStorage` holds zero identity.
- **W2b (`909b163`).** `NotesLibrary` + home redesign: `GET /api/consultations` rendered
  newest-first, grouped by Sofia day, `status`-filterable, paginated („Покажи още"). Rows are the
  visit's auto-generated label and link to the visit itself (result page when filed, scribe when
  in-flight). It subsumes the old today-rail and replaces the patients-history view; `StatusPill`
  / `STATUS_LABEL` / `visitHref` are now shared from here.
- **W3 (`9eda9cb`) + docs (`fabcc30`) + landing (`8c92891`).** The whole patient surface is
  **deleted** — `PatientForm`, `PatientSearch`, `PatientResultRow`, `PatientLoadConfirmModal`,
  `EgnSwitchGuardModal`, `DedupModal`, `RevealEgnButton`, `TodayConsultations`, `lib/egn.ts`,
  `lib/national-id.ts`, `lib/age.ts`, the `/app/patients` page and its `AppShell` nav entry
  (20 files). The landing hero mockups dropped their Пациенти nav item so the marketing site
  stops advertising a surface that no longer exists. Do NOT reintroduce any of it.
- **Investigation-block mirrors — pacemaker (`e75fb45`) + ekg (`3a489e1`, docs `7a85b6b`).**
  `lib/pacemaker-template.ts` and `lib/ekg-template.ts` join the cross-repo mirror invariant
  (display slice of the backend's `pacemaker-v1.js` / `ekg-v1.js`) and register in
  `lib/investigation-blocks.ts`. Both are EMBEDDED-only and are WORKING DRAFTS pending Соколов.
- **Paragraph render mode for light blocks (`9453e5f` → `f6a36c3`).** `renderStyle:'paragraph'`
  (ЕКГ) exports as ONE short prose paragraph instead of label rows — first in the exporters,
  then `f6a36c3` brought the on-screen card onto the **same** builder (`blockParagraph`, exported
  from `lib/exporters.ts`), so screen and export share one source of truth and cannot drift.
  Rows still appear for grid-style blocks, and for paragraph blocks when expanded or flagged.
- **obektivno_findings in the source highlight (`7607487`).** `lib/field-sources.ts` maps
  `obektivno` to `{ exact: ['vitals'], prefix: 'obektivno_findings.' }`, so the per-clause
  provenance keys G7 introduced light up the Обективно highlight instead of only the vitals line.

# Embedded investigation blocks (C4–C7) + trust-layer source UI (2026-07-19/20)

The result page now renders, edits, and exports the backend's `izsledvania_blocks`
(mixed консултация + dictated readout visits), and shows honest field-level provenance.
Contract details live in `tubermed-backend/CLAUDE.md` (output contract) and the new
Known-gotchas bullets here.

- **C4 (`fa929c9`).** `InvestigationBlock` type in `lib/types.ts` + the display registry
  `lib/investigation-blocks.ts` mapping block `type` keys onto `lib/echo-template.ts`
  section lists (the registry holds no descriptors of its own).
- **C5 (`f56a7e2`).** Blocks render as titled cards inside Изследвания on the result page.
- **C6 (`5cc6c7e`).** Dot-path edit round-trip for block fields — `/edit` still posts the
  FULL `fields` object; block edits address `izsledvania_blocks[i].fields` by dot-path.
  (Backend edit-metrics caught up 2026-07-20 — blocks were unmeasured until then.)
- **C7 (`d121584`).** Exporters emit blocks as Изследвания sub-sections in clipboard/PDF/
  Word. Echo polish: `4194ca7` (editing never gated on `isLocked` pre-approval),
  `ae0a0be` (label parenthetical + "ratio" non-unit cleanup, with the backend twin).
- **Trust layer v1.1 UI (`bc40992` + `600c050`).** `FieldSource` contract + stored-offset
  lookup; three honest source states — намерен източник (stored RAW offsets) / няма ясен
  източник / opt-in предположение. Offsets come from the backend's dedicated provenance
  call; the browser never re-derives them.
- **P1 (`898299e`).** Pause/resume within one PC recording (embedded-investigations
  Batch P).

# Colour-token cleanup — `--brand-panel-*` + workspace surface tokens (2026-07-01)

Token hygiene from the 2026-07-01 landing + auth polish batch; no behaviour
change and no colour shift. (The landing `--lp-warn-*` / `--lp-danger-*` feedback
tokens are documented under "Public marketing landing".)

- **`--brand-panel-*` (F5, `79156e3`).** The decorative navy gradient shared by
  `AuthBrandPanel` (the `/app/login` + `/signup` panel) and the `OnboardingWizard`
  `WelcomeBand` was tokenized in the `@theme` block — FOUR navy stops (there were
  four distinct hexes across the two gradients, not three; `#1D3B5C` is a stop in
  both), ordered darkest→lightest: `--brand-panel-deep #16263D` ·
  `--brand-panel-base #1C2B44` · `--brand-panel-mid #1D3B5C` ·
  `--brand-panel-accent #2E5A8F`. `AuthBrandPanel` uses `170deg` deep→base→mid;
  the `WelcomeBand` uses `135deg` deep→mid→accent. Byte-identical to the old
  literals (verified: the resolved gradient is `rgb(22,38,61)/rgb(28,43,68)/
  rgb(29,59,92)`) — a visual no-op. (Tailwind v4 emits these non-namespaced
  `@theme` vars to `:root`; confirmed present in the production CSS even though
  they are only referenced from JSX inline styles.)
- **The scribe QR keeps a LITERAL `#1C2B44`, not `var(--brand-panel-base)`.** The
  phone-pairing `QRCodeSVG` `fgColor` stays literal (with a "mirror
  `--brand-panel-base` — keep in sync" comment): `fgColor` becomes an SVG `fill`
  and a CSS var may not resolve on an export/print path — a broken/low-contrast QR
  would break the pairing capture flow. Keep it literal; keep it in sync.
- **"No gradients" rule scoped (`79156e3`).** The `globals.css` comment went from
  "No gradients in clinical UI" to "No gradients on clinical work surfaces;
  decorative auth/brand panels may use the navy brand gradient (`--brand-panel-*`)".
  `--gradient-brand` still stays a flat `var(--color-accent)` fallback. Clinical
  work surfaces stay flat; the only sanctioned gradient is the decorative
  auth/brand navy panel.
- **Surface literals → `--color-bg-surface` (F7, `9c56de0`).** Two workspace `#fff`
  literals were repointed to `var(--color-bg-surface)`: the accent-tile icon
  foreground in `components/TodayConsultations.tsx` and the active-segment
  background in `components/ui/Segmented.tsx`. Token hygiene only; no visual
  change.

# Auth probe: forward remembered doctors in on a slow/failed `/me` (2026-07-09)

Regression fix for `5038452` (the branded-splash-replaces-white-screen change),
`d3a2b94`. Two coupled symptoms, one broken path: a valid „Запомни ме" session was
forced to re-login, and the branded splash read as a pre-form gate.

- **Root cause.** In `app/app/login/page.tsx` + `app/signup/page.tsx` the auto-forward
  probe's new `PROBE_TIMEOUT_MS = 8000` timeout AND its network-failure/5xx `.catch`
  both called `setProbeFailed(true)` — flipping `probing` false, which renders the
  **login form**. The backend `/me` (`buildMeBody`) runs `authMiddleware` (synchronous
  `jwt.verify` → fast 401) *before* several sequential Supabase round-trips; a cold
  Railway `/me` exceeds 8s, the timeout fires, and a doctor holding a valid 30-day
  token is bounced to the form.
- **Fix.** Only a real **401** now clears the session (`clearSession()` +
  `setProbeFailed(true)` + `return`) and renders the form. **Timeout / network error /
  5xx** now `router.replace("/app/new-visit")` — forward into the app on the
  still-valid stored token. Safe because a timeout implies auth already passed (the
  slowness is the post-auth DB work), and the workspace shell
  (`app/(workspace)/layout.tsx`) gates on `getSession()` alone, so the doctor lands on
  the cached `doctor` profile — no white screen, no bounce.
- **Loader's correct home.** New `authenticating` flag, set only on submit SUCCESS
  (after `setSession`), drives `AuthBootSplash` via `if (probing || authenticating)`.
  The auto-forward effect early-returns while `authenticating` is true (deps include
  it) so the post-submit re-render can't fire a redundant probe. An un-remembered
  doctor (no token) still gets the form immediately — `probing` and `authenticating`
  are both false.
- **Untouched (verified by a fresh-context adversarial review).** `clearSession` (S4)
  still wipes `tuber_auth` from both stores + the PHI keys; sidebar/settings logout
  call sites unchanged; `setSession`/`getSession` remember-me persistence unchanged. A
  401 never reaches a forward path.
- **By-design tradeoff.** If the backend were slow enough to 401 only *after* 8s, the
  timeout forwards that dead token in and the workspace's own `/me` handles it. Relies
  on real 401s returning fast (synchronous JWT verify) — true for the cold-start case.
- **Verified live** (backend down = network-fail branch): no token → form instantly;
  stored token + failed probe → straight into `/app/new-visit`, workspace rendered from
  cached profile, no white screen; logout → both stores cleared → form. Gates: `build`
  + `tsc --noEmit` clean, lint zero new. Left for QA (needs live backend): real 401 →
  form, the post-submit splash on a real login, and the >8s throttle timeout branch.
